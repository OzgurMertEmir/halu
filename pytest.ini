[pytest]
minversion = 7.0
addopts = -ra
testpaths = tests
markers =
    slow: may be slow or use external resources
    integration: multi-module or serialize artifacts
    hf: downloads from Hugging Face (skipped by default)